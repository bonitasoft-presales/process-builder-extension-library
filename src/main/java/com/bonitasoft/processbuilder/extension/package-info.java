/**
 * Provides classes for the custom actor filter based on JSON configuration.
 * The filter assigns tasks to users based on initiator, explicit user lists,
 * and memberships.
 */
package com.bonitasoft.processbuilder.extension;
